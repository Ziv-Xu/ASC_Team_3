## Linux
练习了一些基本的linux指令，ls,mv,cp等，了解了linux系统的文件结构。
尝试自己安装配置好用的工具，如zsh。

## gcc
通过ubuntu的apt下载了环境开发的基本工具（sudo apt install build-essential）,其中包含gcc。
通过gcc --version 查看了gcc的版本,并用gcc编译了一个C程序。
使用make从源码编译了htop(一个只用c语言编写的开源程序)。

## git
了解了git是用与代码版本控制的工具，学习了基本的git指令(git init,git add,git commit,git remote等)。
尝试在github创建测试仓库，并创建main,feature分支，然后通过pull request将feature合并至main。

## 困惑
如果队友负责的项目和自己的发生冲突，该如何处理。
